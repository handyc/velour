// Copy this file to `wifi_secrets.h` in the same directory and fill in
// the WiFi credentials + Velour URL. The real wifi_secrets.h is
// gitignored — it never leaves your machine.
//
// Per-device identity (NODE_SLUG, NODE_TOKEN, NODE_HTTP_PORT, etc.)
// now lives in platformio.ini build_flags + secrets.ini, NOT here.
// This file is only for the shared WiFi network credentials.

#pragma once

// Your Wi-Fi network credentials. The network must be 2.4 GHz —
// ESP8266 does not support 5 GHz.
#define WIFI_SSID     "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"

// The base URL of your Velour instance, reachable from the fleet's
// network position. Examples:
//   "http://192.168.1.50:7777"   <- LAN dev (use the LAN IP, NOT 127.0.0.1)
//   "https://velour.example.com"    <- production behind nginx
// NO trailing slash. NO /api/nodes path. The client appends those.
#define VELOUR_URL    "http://192.168.1.50:7777"
