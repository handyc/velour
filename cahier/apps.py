from django.apps import AppConfig


class CahierConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'cahier'
    verbose_name = 'Cahier — Jupyter notebook catalogue'
