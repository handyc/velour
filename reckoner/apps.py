from django.apps import AppConfig


class ReckonerConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'reckoner'
    verbose_name = "Reckoner — Compute's true cost"
