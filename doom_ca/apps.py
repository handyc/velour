from django.apps import AppConfig


class DoomCaConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'doom_ca'
    verbose_name = 'Doom CA — game on a 4-colour hex CA'
