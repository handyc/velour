from django.apps import AppConfig


class OuroborosConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'ouroboros'
    verbose_name = 'Ouroboros — the class-4 fixed-point quine'
