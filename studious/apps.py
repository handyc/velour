from django.apps import AppConfig


class StudiousConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'studious'
    verbose_name = 'Studious (scholar library + argument workshop)'
