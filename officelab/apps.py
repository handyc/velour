from django.apps import AppConfig


class OfficelabConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'officelab'
    verbose_name = 'Officelab — minimal-C office suite analyser'
