"""Optikon — placeholder for future Preset model.

Phase 1 has no DB-backed state; every illusion is computed on the
fly from the params in the URL/form.  Reserve `optikon.Preset` (a
named (illusion, params) tuple) for Phase 2 when users want to save
favourites and re-print them.
"""
