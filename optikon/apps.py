from django.apps import AppConfig


class OptikonConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'optikon'
    verbose_name = 'Optikon — hex-grid optical illusions'
