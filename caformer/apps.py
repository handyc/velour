from django.apps import AppConfig


class CaformerConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'caformer'
    verbose_name = 'CAformer — a transformer made of cellular automata'
