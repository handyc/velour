from django.apps import AppConfig


class ViralystConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'viralyst'
    verbose_name = 'Viralyst — guided IDE for tiny historical programs'
