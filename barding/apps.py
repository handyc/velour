from django.apps import AppConfig


class BardingConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'barding'
    verbose_name = 'barding (Claude Code CLI config)'
